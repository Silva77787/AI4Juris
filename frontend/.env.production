VITE_API_URL=https://ai4juris-backend.onrender.com
```

**Passo 4:** Adiciona ao `.gitignore` (se ainda não estiver):

No ficheiro `frontend/.gitignore`, certifica-te que tens:
```
.env.local
.env.*.local